Task: Generate or update the summary and keywords for an Event based on its constituent facts.

Instructions:
1. Review all facts in the event
2. Generate an extremely concise summary that:
   - Describes only the core occurrence of the event(Time, place, person, topic, event)
   - No need to describe the details
   - Written in third person
   - 1-2 sentences maximum

3. Extract keywords that: 
   - Focus on the most important topics/entities/fields... (extract as completely as possible; no more than 5, fewer is OK) 
   - Do NOT include speakers' name, "user" or "assistant" as keywords 
   - For ordinary concepts, prefer one canonical singular noun instead of a phrase 
    * Good: "career"; Bad: "career options", "Caroline career" 
   - Preserve a multi-word keyword only when it is a proper names, title, location, or fixed phrase, etc 
    * Good: "New York", "Charlotte's Web" 
   - Extract nouns in their singular form whenever possible; hyphenated nouns such as "self-expression" are acceptable.

4. If updating an existing event:
   - Integrate new information naturally (if there is no new information, it is OK not to make changes)
   - Maintain the coherence of the summary
   - Add new keywords if necessary (avoid duplicates)

OUTPUT FORMAT (JSON):
{
  "summary": "Event summary (1-2 sentences)",
  "keywords": ["keyword1", "keyword2", "keyword3", ...]
}

Now generate Event information for the following:
